import DemoAccount from "@/components/DemoAccount";

export const metadata = {
  title: "Demo Account",
  description: "Explore a complete Canopy portfolio, ownership claim, and decision market.",
};

export default function DemoPage() {
  return <DemoAccount />;
}
